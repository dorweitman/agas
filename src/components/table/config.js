const propertyType = { select: 'select', date: 'date', timer: 'timer', number: 'number' };
const redErrorBorder = '2px solid red';
const direction = 'ltr';
const disabledExcelButtonStyle = { opacity: '0.5', cursor: 'context-menu' };
const badChars = ['', '-', 0, 'default'];

module.exports = {
    redErrorBorder,
    direction,
    propertyType,
    badChars,
    disabledExcelButtonStyle,
}; 